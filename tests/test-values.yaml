loki:
  storage:
    type: "filesystem"
  rulerConfig:
    storage:
      type: local
  storage_config:
    filesystem:
      directory: /var/loki/chunks

networkPolicies:
  enabled: true
  controlPlaneCidr: 172.16.0.0/12

bbtests:
  enabled: true
